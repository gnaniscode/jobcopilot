# Local Data

Place your resume here as `resume.pdf` or `resume.docx`.

This folder is gitignored to prevent accidentally committing personal data.

## Files

- `resume.pdf` / `resume.docx` — your input resume (gitignored)
- `_last_failed_response.txt` — debug dump if Claude returns invalid JSON (gitignored)